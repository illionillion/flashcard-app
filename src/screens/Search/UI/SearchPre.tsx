import { FC } from "react";
import { Text, View } from "react-native";

/**
 * 単語検索画面のUI
 */
export const SearchPre: FC = () => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>単語検索</Text>
    </View>
  );
};
